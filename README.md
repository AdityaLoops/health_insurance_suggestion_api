# Health Insurance Price Estimator

A machine learning-powered web application that estimates health insurance charges based on user-provided personal and demographic information.

Built as a learning project while exploring **FastAPI** and **serverless deployment with Vercel**.

## 🚀 Live Demo

**[Health Insurance Price Estimator](https://health-insurance-suggestion-api.vercel.app/)**

## 📌 About the Project

The application takes information about an individual and uses a trained **Random Forest Regressor** to estimate their annual health insurance charge.

Users can provide:

- Age
- Sex
- BMI
- Number of children
- Smoking status
- Region

The prediction is generated by the machine learning model and displayed directly on the web interface.

> **Note:** This project is intended for educational and demonstration purposes. The prediction is not a real insurance quote or financial/medical recommendation.

## 🧠 Machine Learning

The application uses a **Random Forest Regressor** trained on health insurance data.

The trained model is saved using `joblib` and loaded by the FastAPI backend when the application runs.

### Input Features

| Feature | Description |
|---|---|
| Age | Age of the individual |
| Sex | Male or female |
| BMI | Body Mass Index |
| Children | Number of children |
| Smoker | Whether the individual smokes |
| Region | Geographic region |

### Output

The model returns an estimated annual insurance charge in **USD**.

## ⚡ FastAPI Backend

The backend is built using **FastAPI**.

### API Endpoints

| Method | Endpoint | Description |
|---|---|---|
| GET | `/api/` | API status |
| GET | `/api/health` | Health check |
| POST | `/api/predict` | Generate an insurance cost prediction |

FastAPI also provides automatically generated interactive API documentation through Swagger UI.

### Swagger Documentation

**[Open API Documentation](https://health-insurance-suggestion-api.vercel.app/api/docs)**

## 🎨 Frontend

The frontend is built using:

- HTML
- CSS
- JavaScript

The frontend collects the user's information and sends it to the FastAPI backend using the JavaScript **Fetch API**.

The returned prediction is then displayed on the page.

## 🔄 Application Flow

```text
User
  ↓
Frontend Form
  ↓
JavaScript Fetch API
  ↓
POST /api/predict
  ↓
FastAPI
  ↓
Pydantic Validation
  ↓
Pandas DataFrame
  ↓
Random Forest Regressor
  ↓
Predicted Insurance Charge
  ↓
JSON Response
  ↓
Frontend
```

## 🛠️ Tech Stack

### Machine Learning
- Python
- Pandas
- Scikit-learn
- Joblib

### Backend
- FastAPI
- Pydantic
- Uvicorn

### Frontend
- HTML
- CSS
- JavaScript

### Deployment
- Vercel

## 📁 Project Structure

```text
health_insurance_suggestion_api/
│
├── api/
│   └── index.py
│
├── public/
│   ├── index.html
│   ├── script.js
│   └── style.css
│
├── insurance.csv
├── insurance_model.joblib
├── explore.py
├── train_model.py
├── main_local.py
├── requirements.txt
└── README.md
```

## ▶️ Run Locally

### 1. Clone the repository

```bash
git clone https://github.com/AdityaLoops/health_insurance_suggestion_api.git
cd health_insurance_suggestion_api
```

### 2. Create a virtual environment

```bash
python -m venv venv
```

### 3. Activate the virtual environment

Linux/macOS:

```bash
source venv/bin/activate
```

Windows:

```bash
venv\Scripts\activate
```

### 4. Install dependencies

```bash
pip install -r requirements.txt
```

### 5. Start the FastAPI server

```bash
uvicorn api.index:app --reload
```

The API will be available at:

```text
http://127.0.0.1:8000
```

Swagger documentation will be available at:

```text
http://127.0.0.1:8000/api/docs
```

## 📚 What I Learned

This project was built primarily as a learning project while exploring backend development and deployment.

Through this project, I worked with:

- Building REST APIs using FastAPI
- Creating API endpoints
- Request validation using Pydantic
- Connecting a machine learning model to an API
- Loading a trained model using Joblib
- Converting API input into a Pandas DataFrame
- Sending JSON data between JavaScript and Python
- Using the JavaScript Fetch API
- Handling CORS
- Connecting a frontend with a backend
- Understanding serverless functions
- Deploying a FastAPI application using Vercel
- Serving a frontend alongside an API
- Taking a locally working ML application and making it publicly accessible

## 🚧 Future Improvements

Some possible improvements for the project include:

- Improving model evaluation and analysis
- Adding model performance metrics
- Providing more detailed prediction insights
- Improving input validation and error handling
- Adding more informative visualizations
- Further improving the UI/UX
- Exploring additional machine learning models
- Adding automated testing

## ⚠️ Disclaimer

This project is intended for **educational and demonstration purposes only**.

The predicted insurance charge should not be considered a real insurance quote, financial advice, or medical recommendation.

## 👨‍💻 Author

**Aditya Raj Verma**

Built while learning **FastAPI, machine learning deployment, and Vercel**.
